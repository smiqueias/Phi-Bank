import 'package:flutter/material.dart';
import 'package:phi_mobile_challenge/src/features/statement/view/container/statement_container.dart';

class StatementRouter extends StatelessWidget {
  const StatementRouter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const StatementContainer();
  }
}
